# NEXUS TRADER - 項目結構說明

## 📁 完整目錄結構

```
nexusv3/
├── 📄 配置文件
│   ├── .eslintrc.cjs          # ESLint 配置
│   ├── .gitignore             # Git 忽略文件
│   ├── index.html             # HTML 入口
│   ├── package.json           # 項目依賴和腳本
│   ├── postcss.config.js      # PostCSS 配置
│   ├── tailwind.config.js     # Tailwind CSS 配置
│   ├── tsconfig.json          # TypeScript 主配置
│   ├── tsconfig.node.json     # TypeScript Node 配置
│   └── vite.config.ts         # Vite 構建配置
│
├── 📄 文檔
│   ├── README.md              # 項目介紹
│   ├── SETUP.md               # 安裝和使用指南
│   └── PROJECT_STRUCTURE.md   # 本文件
│
├── 📁 public/                 # 靜態資源
│   └── vite.svg               # Vite 圖標
│
└── 📁 src/                    # 源代碼目錄
    ├── main.tsx               # 應用入口點
    ├── App.tsx                # 主應用組件 (路由管理)
    ├── index.css              # 全局樣式
    │
    ├── 📁 components/         # 可重用 UI 組件
    │   ├── Badge.tsx          # 徽章組件
    │   ├── Card.tsx           # 卡片容器組件
    │   ├── InputField.tsx     # 輸入框組件 (支持密碼顯示/隱藏)
    │   ├── LanguageSwitcher.tsx  # 語言切換組件
    │   ├── NeonButton.tsx     # 霓虹按鈕組件
    │   └── index.ts           # 組件統一導出
    │
    ├── 📁 views/              # 頁面視圖組件
    │   ├── LoginView.tsx      # ✅ 登入頁面
    │   ├── DashboardView.tsx  # ✅ 主控台頁面
    │   ├── ProfileView.tsx    # ✅ 個人資料頁面
    │   └── index.ts           # 視圖統一導出
    │
    ├── 📁 constants/          # 常量和配置
    │   ├── translations.ts    # 多語言翻譯 (繁中/英文/越南文)
    │   └── mockData.ts        # 模擬數據
    │
    ├── 📁 types/              # TypeScript 類型定義
    │   └── index.ts           # 全局類型定義
    │
    └── 📁 utils/              # 工具函數 (待實現)
```

## 🎯 核心文件說明

### 入口文件

#### `src/main.tsx`
應用的入口點，負責將 React 應用掛載到 DOM。

```typescript
import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
```

#### `src/App.tsx`
主應用組件，負責：
- 視圖路由管理
- 全局狀態管理 (語言、用戶信息)
- 視圖渲染邏輯

### 組件系統

所有可重用組件都位於 `src/components/` 目錄：

| 組件 | 功能 | Props |
|------|------|-------|
| `NeonButton` | 霓虹風格按鈕 | variant, onClick, disabled, fullWidth |
| `InputField` | 輸入框 | icon, type, isSecure, label, subLabel |
| `Card` | 卡片容器 | children, className |
| `Badge` | 標籤徽章 | type, text |
| `LanguageSwitcher` | 語言切換器 | currentLang, setLang |

### 視圖系統

#### 已實現視圖

1. **LoginView** - 登入頁面
   - 帳號/密碼輸入
   - 驗證碼輸入
   - 語言切換
   - 前往註冊/忘記密碼

2. **DashboardView** - 主控台
   - 資產概覽 (可用/凍結/今日交易)
   - 三個交易區域 (平台/商戶/會員)
   - 交易列表 (支持排序和篩選)
   - 快速操作區 (商城/錢包/設定/公告)

3. **ProfileView** - 個人資料
   - 頭像上傳和編輯
   - 暱稱編輯
   - 交易記錄入口
   - 收款帳戶管理
   - 推廣中心
   - 會員權益
   - 安全中心
   - 客服中心

#### 待實現視圖

詳見 `SETUP.md` 中的「待實現功能」章節。

### 常量和配置

#### `constants/translations.ts`
包含三種語言的完整翻譯：
- 繁體中文 (zh-TW)
- 英文 (en-US)
- 越南文 (vi-VN)

結構示例：
```typescript
{
  'zh-TW': {
    appName: "NEXUS TRADER",
    common: { confirm: "確認", ... },
    login: { loginBtn: "登入", ... },
    // ... 更多章節
  }
}
```

#### `constants/mockData.ts`
包含模擬數據：
- 用戶資產數據
- 交易列表 (平台/商戶/會員)
- 交易記錄
- 通知和公告
- 推薦記錄
- 會員等級配置

### 類型定義

`types/index.ts` 包含所有 TypeScript 類型定義：
- `UserInfo` - 用戶信息
- `TransactionItem` - 交易項目
- `ListItem` - 列表項目
- `Notification` - 通知
- `Announcement` - 公告
- `Language` - 語言類型
- `ViewName` - 視圖名稱
- `ViewProps` - 視圖通用 Props

## 🎨 樣式系統

### Tailwind CSS
項目使用 Tailwind CSS 作為主要樣式框架。

配置文件：`tailwind.config.js`

### 全局樣式
`src/index.css` 包含：
- Tailwind 基礎導入
- 自定義滾動條樣式
- 動畫關鍵幀定義
- 全局樣式重置

### 設計系統

#### 顏色主題
```css
主色調：emerald (綠色) - 成功/確認操作
危險色：rose (玫瑰紅) - 刪除/警告操作
信息色：blue (藍色) - 信息/提示
背景色：slate-900 (深灰) - 主背景
卡片色：slate-800 (灰) - 卡片背景
```

#### 動畫效果
- `fade-in` - 淡入
- `slide-in-from-right` - 從右滑入
- `slide-in-from-bottom` - 從下滑入
- `zoom-in` - 縮放進入

## 🔧 開發工作流

### 1. 安裝依賴
```bash
cd nexusv3
npm install
```

### 2. 啟動開發服務器
```bash
npm run dev
```

### 3. 構建生產版本
```bash
npm run build
```

### 4. 預覽生產構建
```bash
npm run preview
```

### 5. 代碼檢查
```bash
npm run lint
```

## 📦 依賴說明

### 生產依賴
- `react` ^18.2.0 - UI 框架
- `react-dom` ^18.2.0 - React DOM 渲染
- `lucide-react` ^0.294.0 - 圖標庫

### 開發依賴
- `vite` ^5.0.8 - 構建工具
- `typescript` ^5.2.2 - TypeScript 支持
- `tailwindcss` ^3.3.6 - CSS 框架
- `@vitejs/plugin-react` ^4.2.1 - React 插件
- `eslint` - 代碼檢查

## 🚀 擴展指南

### 添加新視圖
1. 在 `src/views/` 創建新組件
2. 在 `src/views/index.ts` 導出
3. 在 `src/App.tsx` 的 `renderView()` 添加路由
4. 在 `src/types/index.ts` 添加 ViewName

### 添加新組件
1. 在 `src/components/` 創建新組件
2. 在 `src/components/index.ts` 導出
3. 在需要的地方導入使用

### 添加新翻譯
在 `src/constants/translations.ts` 的三個語言對象中添加相應的鍵值對。

### 添加新類型
在 `src/types/index.ts` 中定義新的 TypeScript 類型或接口。

## 📝 注意事項

1. **類型安全**：所有組件都使用 TypeScript，確保類型正確
2. **響應式設計**：使用 Tailwind 的響應式工具類
3. **可訪問性**：確保所有交互元素都有適當的 ARIA 屬性
4. **性能優化**：使用 React.memo 和 useMemo 優化重渲染
5. **代碼風格**：遵循 ESLint 規則，使用 Prettier 格式化

## 🔗 相關資源

- [React 文檔](https://react.dev/)
- [TypeScript 文檔](https://www.typescriptlang.org/)
- [Vite 文檔](https://vitejs.dev/)
- [Tailwind CSS 文檔](https://tailwindcss.com/)
- [Lucide Icons](https://lucide.dev/)

---

**更新日期：** 2024-12-18  
**版本：** 1.0.0

